# Codex adapter

Codex discovers the repository root `AGENTS.md`. Read it first, then `.context/INDEX.md`, the selected conversation profile, intake classification and active work item. Canonical rules remain in `.context/`; this adapter adds no policy.
