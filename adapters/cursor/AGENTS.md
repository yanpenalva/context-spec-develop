# Cursor adapter

Cursor discovers the repository root `AGENTS.md` and `.cursorrules`. Read `AGENTS.md` first, then `.context/INDEX.md`, the selected conversation profile and the active work item. Canonical instructions remain in `.context/`; this adapter adds no project-specific policy.
