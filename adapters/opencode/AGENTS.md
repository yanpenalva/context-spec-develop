# OpenCode adapter

OpenCode reads `opencode.jsonc` and discovers the repository `AGENTS.md`. Read it first, then `.context/INDEX.md`, the selected conversation profile and the active work item. Canonical instructions remain in `.context/`; this adapter adds no project-specific policy.

