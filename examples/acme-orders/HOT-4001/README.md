# HOT-4001 — Prevent duplicate shipment webhook processing

Hotfix example: a minimal spec, targeted test, explicit rollback trigger and post-deploy observation were required; a full postmortem captured the idempotency gap before closure.
