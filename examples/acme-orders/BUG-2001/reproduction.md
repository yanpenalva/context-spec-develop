# Reproduction — BUG-2001

Observed: timestamp is shown without its timezone label. Expected: the configured timezone label remains visible. Reproduction uses a non-production fixture at the daylight-saving boundary.
