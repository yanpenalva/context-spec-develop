# Specification — FEAT-1001

Show a timezone-aware delivery window on the order status page. Out of scope: carrier selection and notification behavior. The UI must handle missing estimates without inventing a date.
