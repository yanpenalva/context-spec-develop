# FEAT-1004 — Discount field on order lines

Demonstrates reclassification: intake classified the change as `standard` (single module), and planning evidence — the orders webhook publishes order lines to partners — materially changed impact, security and complexity. The `reclassification` entry in `work-item.json` records the previous dimensions, the trigger and the routing impact per `.context/classification/README.md`; the live `classification` object now states the present dimensions and routes the item `extended`.

## Outcome record

Planning continues at extended depth: deep plan with dependency analysis, independent review and security review before release. The example ends at the plan phase to show the recorded reclassification itself.
