# Plan — <ID> <TITLE>

## Approved approach

Describe the smallest viable change and why it fits the existing system.

## Confirmed touchpoints

- Files/components/services:
- Interfaces and data:
- Dependencies:

## Execution order

1.

## Subtasks and waves

| Subtask ID | Owner | Dependencies | Acceptance evidence | Wave | Context domains |
| --- | --- | --- | --- | --- | --- |
| `<SUBTASK>` | `<OWNER>` | `<DEPENDENCIES>` | `<EVIDENCE>` | `1` | `core,project` |

Each row is the compact planner→executor handoff: it carries the bounded task, its dependencies, the acceptance evidence and only the context domains that subtask needs (see `.context/context-routing/`). Parallel work is allowed only when mutable boundaries do not conflict. Name the integration owner and subagent handoff for each wave.

## Test plan

-

## Risks and rollback

- Risk:
- Mitigation:
- Rollback:

## Required approvals

-
