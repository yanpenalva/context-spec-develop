# Outcome: FEAT-9012

Last two startup-mandatory Git references removed (policy bullet + README Git delivery). Documentation now uniformly states: early preference reused, otherwise deferred, confirm_each safe fallback, unresolved never authorizes. Follow-up: none.
