# Discovery: FEAT-9014

Owner request (2026-09-21): add an MCP adapter to the package so MCP clients (ChatGPT included) can drive CSD over a local repository without duplicating canonical contracts. Phase 1 findings: zero existing MCP references in the repository; core is ~518 lines (`cli.ts`, `installer.ts`, `fs.ts`, `types.ts`, `adapters.ts`); no work-item API exists in code — all `.context/work/` mutation is agent-performed today; validation exists only as `scripts/validate_context.py` (Python, no dependencies); `fs.ts` has atomic writes but no realpath/symlink protection; `ownedPath` containment is private to `installer.ts`; `Adapter` type assumes a `skillPath` and therefore must not absorb MCP; package has zero runtime dependencies (MCP SDK becomes the first). User approved decisions D1–D8: official MCP SDK; two-tool bootstrap gate with stateless preview token; `csd_context` returns bootstrap plus references only; minimal structural validation on work-item writes with a closed artifact allowlist; `--root` pins the server when provided; MCP documented as an integration, not an `Adapter`; `csd_validate` invokes the Python validator with fixed argv plus a new additive `--json` flag; missing work-item id resolves to all open items.
