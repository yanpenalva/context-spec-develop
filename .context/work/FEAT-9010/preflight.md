# Preflight: FEAT-9010

- Spec/plan agree with owner request: ordering + decision timing only; classifier architecture, schemas and validator structure untouched.
- Resolution semantics defined for profile (explicit > recorded > default, ask only material), Git mode (phase-relevant; unresolved ≠ authorization; confirm_each fallback), roles (defaults, override on request/invalidity).
- Session decisions reused, not re-asked: profile senior-software-engineer, git confirm_each (recorded on this work item) — live application of the new rule.
- READY.
