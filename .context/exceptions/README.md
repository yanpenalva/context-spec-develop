# Approved Exceptions

Store one JSON file per exception in this directory. Use `.context/templates/common/exception.json`. Exceptions are versioned evidence and should contain no secrets or personal data.
